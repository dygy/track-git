{
  "name": "track-git",
  "version": "1.0.0",
  "description": "track info from git repos",
  "main": "tracker.ts",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node ./build/index",
    "start:dev": "npx nodemon",
    "complie": "npx tsc ./src/tracker.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "yarn@1.22.19",
  "keywords": [
    "git"
  ],
  "devDependencies": {
    "@types/node": "^20.6.3",
    "nodemon": "^3.0.1",
    "prettier": "3.0.3",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "5.2.2"
  },
  "author": "Arkadii Shwartcman",
  "license": "ISC"
}
